# Client-side environment variables
# Replace values with your project's settings. Never add production service_role keys here.

# Vite API URL used by axios client (fallback to http://localhost:3000/api)
VITE_API_URL=http://localhost:3000/api

# Supabase project values (required for Supabase integration)
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-public-anon-key

# Optional: feature flags or env values you may want in future
# VITE_ENABLE_SOCIAL_LOGIN=true
# VITE_SENTRY_DSN=your_sentry_dsn
